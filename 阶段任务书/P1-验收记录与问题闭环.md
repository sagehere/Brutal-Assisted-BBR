# P1 阶段验收记录与问题闭环

日期：2026-09-19；阶段：规格收敛与基线冻结；结论：**P1 FINAL CLOSED / G1 FINAL GO，可进入阶段 02 Observe 实现**。

`p1-baseline-v2` 的复核缺口已经由 Final Closure 全部闭环：预算记账统一为单 datagram 预扣且 socket 失败不退款；当前宿主 guard 统一为 LossDetected/PtoFired；persistent-congestion/ECN-CE 明确为 unsupported；补齐 admission/deadline 参数、reason-code registry、参数来源和 C01-C15 完整验收矩阵。`P1 Final Gate` run `35411680134` 已成功，因此 `p1-baseline-v3` 正式冻结。

## 任务完成情况

| 任务 | 状态 | 证据 |
| --- | --- | --- |
| S1-01 | 完成 | 固定宿主、版本、许可证、构建依赖和可复现 `P1 Gate` 已冻结；run `35409880531` 成功 |
| S1-02 | 完成 | [宿主与环境记录](P1-宿主与环境记录.md) 与 [恢复/I/O 事件桥接设计](P1-恢复与IO事件桥接设计.md)：真实 BBR 相位、Loss/PTO、路径、socket 边界与 unsupported capability 均已闭环 |
| S1-03 | 完成 | [修订规格](P1-修订规格与冻结基线.md) §2 |
| S1-04 | 完成 | [修订规格](P1-修订规格与冻结基线.md) §3；参数 JSON；C01、C02、C03、C05 |
| S1-05 | 完成 | [修订规格](P1-修订规格与冻结基线.md) §4；C01、C04、C05、C06 |
| S1-06 | 完成 | [修订规格](P1-修订规格与冻结基线.md) §5；参数 JSON |
| S1-07 | 完成 | 冻结配置、主要/次要场景、指标与日志预算 |
| S1-08 | 完成 | 本记录的追踪表；v0.2 替代声明 |

## 复现命令与结果

```powershell
python 阶段任务书/check_p1_rules.py
```

此检查只使用 Python 标准库并验证确定性合成事件；它不产生网络包，也不是吞吐、延迟或公平性证据。Final Closure 预期输出为 C01-C15 共十五项 `PASS`，并另运行 20 项规格一致性检查。

```powershell
git -C third_party/quiche-0.29.3 rev-parse HEAD
```

实际输出：`55886df3be579579207104c8e645825b6347a209`。

原 Windows/WSL 失败保留为历史证据，不再作为最终门禁。当前权威入口为：

```bash
bash "阶段任务书/verify_p1.sh"
```

以及 `.github/workflows/p1-gate.yml`。自动门禁使用 Ubuntu 24.04 + Rust 1.88，校验固定 quiche SHA，运行九项合成规则检查以及带 `gcongestion` 的 quiche 库测试，并上传工具版本、完整测试日志与 SHA-256 证据。

## R2 问题闭环与阶段 02 用例

| R2 | 修订条款 | P1 检查 | 阶段 02 回归 |
| --- | --- | --- | --- |
| P0-1 不可达退出/无 ACK | 规格 §3-4：时间、预算、无收益与发送入口 | C01、C05 | L03、L05 |
| P0-2 相对基线增幅 | 规格 §3：`P_cap ≤ 1.10×B_ref` | C02 | L02 |
| P0-3 W=0/CWND | 规格 §1、§3 | C03 | L01 |
| P0-4 基线状态与学习 | 规格 §1、§3-4；宿主表 | C04、C08 | L04、L10 |
| P1-1 去重计量 | 规格 §2 | 后续协议事件回放 | L06 |
| P1-2 效率因果性 | 规格 §2；η 仅遥测 | C07 | L07 |
| P1-3 控制时钟 | 规格 §3：每 BBR round 一次 | C08 | L11 |
| P1-4 状态一致性 | 规格 §4 唯一表 | C01-C06 | L01-L05、L08-L09 |
| P1-5 RTT/路径/ECN | 规格 §3-4；宿主表 | C04、C06 | L04、L07、L09 |
| P1-6 公平与聚合 | 规格 §5 | 配置审查 | 阶段 03 G 组 |

## 合成用例说明

| 编号 | 事件 | 期望结果 |
| --- | --- | --- |
| C01 | 150 Mbps 可交付、200 Mbps Target、RTT 从 50 到 52 ms、两个 round 无 2% 模型收益 | 无收益退出并退避，不等待 RTT Hard |
| C02 | 20 Mbps 基线、1000 Mbps Target | 初始输出不超过 20.1 Mbps，不产生旧公式的 265 Mbps 跳变 |
| C03 | W=1 后关闭 Target；基线 CWND 4,000 byte | 输出严格为基线 pacing 和 CWND |
| C04 | Startup、Drain、ProbeRTT、ProbeBW.Down、LossDetected、PtoFired | 保护相位撤销辅助；Loss/PTO 撤销并进入退避 |
| C05 | 无 ACK 超时、已用尽字节预算、不可撤销队列 | 计时/预算停止辅助，队列容差固定为 2,400 byte |
| C06 | Target 关闭、路径变化、mode 切换 | 都保留既有退避截止 |
| C07 | 过期样本、零基线、反馈不齐 | 输入无效，不加码、不除零 |
| C08 | 有效 delivery 增长 | 可继续基线学习；Lite 不改 CWND，撤销后基线可超过 Target |

## 剩余门禁与 G1 判定

P1 设计未决项已经闭环：

1. `BBRv2::is_in_recovery()` 明确禁止作为 BABR 安全输入；Loss/PTO 从 recovery/loss-detection 层桥接。
2. 固定宿主没有可用的 persistent-congestion/ECN-CE 控制信号，两项能力显式冻结为 unsupported，不创建替代控制律。
3. 执行预算使用 datagram 生成预扣且失败不退款；实际发送遥测使用 socket/GSO 成功返回字节，二者不混用。
4. 构建、规则检查、工具版本和 SHA-256 由 `P1 Gate` 在干净环境自动产出。

最终门禁已满足：

- `P1 Final Gate` run：`35411680134`，结论 `success`。
- 固定 quiche HEAD：`55886df3be579579207104c8e645825b6347a209`。
- P1 合成检查：**C01-C15 全部 PASS**。
- 规格一致性检查：**S01-S20 全部 PASS**，`P1 spec consistency PASS`。
- quiche：`cargo test ... --features gcongestion`，**1048 passed, 0 failed**。
- Final evidence artifact：`10574453438`，digest `sha256:eccb3fb67e292f95fd88f253b89ff65b35b67accd41fca6d8a82f12d214d238f`。
- Final Gate HEAD：`0a888d523065a82e748823531f7ff825c15f8f61`。
- v3 关键文件与固定上游源文件的 SHA-256 已写入 artifact。

门禁过程中发现并闭环两项非算法缺陷：初始仓库缺少 `.gitmodules`；C01 测试夹具错误继承了 21 MB/s delivery，与“无 2% 收益”描述冲突。两者均修复后重验通过，没有降低任何安全阈值。

**G1 = FINAL GO。** P1 已 Final Closed。下一阶段仍只能先实现 Observe；阶段 02 的网络命名空间、netem/tbf 校准、L 系列回归和 Observe 无扰动检查通过后，才允许受控 BABR-Lite 发包。Final G1 不代表 BABR 已证明性能收益。
