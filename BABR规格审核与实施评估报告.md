# BABR 规格审核与实施评估报告

审核对象：`Brutal-Assisted BBR（BABR）拥塞控制算法设计规格.md`，v0.2，Draft / Experimental。  
审核日期：2026-09-18。  
报告修订：R2（综合第三方意见复核）。本轮补充观测模式、Target 配置语义、采样有效性、预算执行、状态原因码和定向回归；立项结论不变。  
审核方式：全文静态评审、公式与状态机反例分析，以及 BBR、QUIC、Hysteria 官方资料核对。未实现算法、未运行网络实验；本文的性能收益、工期和验收阈值均不是实测结论。

## 1. 决策结论

**建议有条件投入小规模实验原型；不建议按 v0.2 直接开展生产实现或作为公网默认拥塞控制器。**

| 评估项 | 判断 | 决策依据 |
| --- | --- | --- |
| 研究与实验价值 | 中等，值得验证 | 可验证目标感知的短时主动探测是否改善容量恢复和随机丢包下的吞吐 |
| 相比现有 BBR 的增量价值 | 尚未证明 | BBR 已主动探测；必须证明收益并非单纯增加攻击性、修复基线缺陷或挤占其他流量 |
| 用户态原型可行性 | 较高，但取决于宿主接口 | 需复用成熟 BBR、交付采样器、pacer 和恢复逻辑 |
| 按当前规格独立实现的一致性 | 低 | 计量口径、控制时钟、状态优先级和退出上界缺失，不同实现会产生不同算法 |
| 生产可行性 | 目前证据不足 | 有不退出、破坏排队消退、覆盖安全窗口等阻断项；没有性能和公平性数据 |

适合首先定位为：**有发送需求时，在明确预算内进行目标触发的额外探测**。Target 表达的是探测意愿，不能证明网络存在额外容量，也不能代表连接应当获得的公平份额。

## 2. 实施价值

### 2.1 值得保留的设计

规格明确 Target 不是速率上限、不是吞吐保证；网络模型持续运行；关注 goodput 而非写入速率；对应用受限、Startup、ProbeRTT 做保护；包含迟滞、退避及成本指标。这些方向合理，尤其是明确要求额外流量必须带来真实收益。

潜在受益场景是持续有数据的长连接、容量恢复后基线重新探测较慢、具有真实剩余容量且存在非拥塞丢包的路径。短请求、应用供给不足、接收端流控受限或 CPU/pacer 受限场景，不应期待通过提高网络发送目标获得改善。

### 2.2 需要先证明的商业与工程假设

没有 Target 的 BBR 本来就追求利用可用带宽。因此，“用户希望 200 Mbps”自身不构成新容量信息。BABR 的价值只能来自更合适的探测时机、探测预算或损失处理，不能从算法名称直接推出。

建议先回答三个问题：

1. 目标宿主的原版 BBR 在什么可复现场景长期低于目标？根因是控制策略，还是版本缺陷、接收窗口、应用供给或实现性能？
2. 更简单的基线参数修正或短时探测增益是否已经解决问题？
3. 加入 `T/A` 后，是否显著优于不含损失补偿的 BABR-Lite？

Hysteria 官方配置说明当前列出的 BBR 为 v1，并明确说明 Brutal 损失补偿可能无效甚至恶化连接。因此必须锁定实际依赖版本，不能把某宿主中的 BBR 当作所有 BBR 版本的代表。[Hysteria 客户端配置](https://hysteria.network/docs/advanced/Full-Client-Config/)

### 2.3 投入边界

优先投入可复现实验和遥测，不宜先投入 Profiles、Adaptive Target、Confidence Model 或跨协议移植。若收益只存在于挤占其他流量的竞争场景，或者成熟基线能获得同等收益，应停止把 BABR 作为通用拥塞控制方案推进。

## 3. 必须修正的规格问题

本文 P0 表示进入实际发包原型前必须解决；P1 表示进入性能结论与扩大试验前必须解决；P2 表示规格整理项。

### P0-1：目标不可达时，没有保证退出的控制路径

位置：§35–41、§49–51、§71、§75 Invariant 3。

HardCongestion 的两个分支都需要 RTT 膨胀。考虑限速器直接丢弃超额流量、或者浅队列路径：

```text
Target = 200 Mbps
实际可交付上限 = 150 Mbps
MinRTT = 50 ms，SRTT = 52 ms
提高 pacing 后 Delivery 仍约为 150 Mbps
```

此时 `D < 0.8T`，可以进入 Assist；但 `Q_ratio = 1.04`、`Q_delay = 2 ms`，即使边际效率趋近 0，HardCongestion 仍为 false。A 下降又会提高 P_target。若发送端能够实际执行该速率，Assist 可以长期停留；LossTrend 停止上升后，Soft 条件也未必成立。

这不是断言所有 BBR 宿主都会出现该现象，而是规格没有排除这个可达状态，因而无法保证“不可能的目标最终退出”。

**修订要求：**每次 Assist 必须有最大持续时间、累计额外发送预算和独立于 RTT 的无收益退出条件。预算或无收益退出也计入失败；重复失败进入抑制状态。RTT 平稳不等于没有拥塞，RTT 不能成为所有退出路径的必要条件。

预算执行不能只依赖 ACK 回调：采用单调时钟截止时间，并在发送入口检查；ACK 停止时仍须到期禁用辅助，保留宿主合法的恢复探测。单次预算的参考速率在进入该轮探测时确定，不能随着 P_final 或新带宽样本上升自动抬高，以免形成“越发越获准多发”的循环。首版难以准确计量额外发送时，可用该轮实际总发送字节上限保守替代；它是执行预算，不是相对于未启用 Assist 的真实额外流量统计。

### P0-2：MaxAssistGain 并未限制相对基线的增益

位置：§12–17、§29、§66、§96。

`P_target <= 1.25T` 限制的是相对用户目标的损失补偿，不是相对当前网络模型的额外注入。反例：

```text
P_bbr = 20 Mbps，T = 1,000 Mbps，A = 1
第一轮 W = 0.25
P_final = 20 + 0.25 × (1,000 - 20) = 265 Mbps
```

第一次 Assist 就达到基线的 13.25 倍；W=1 时达到 50 倍。连续权重只避免直接跳到目标，并不构成安全的增长界限。

**修订要求：**将当前参数改名为 `TargetCompensationCap`，另设相对可信带宽/基线的探测上限、每轮最大增幅和字节预算。所有上限都应在发包执行路径上生效；不能只限制 W。超大或远程传入的 Target 还需配置校验和本地策略限额。

### P0-3：CWND 公式在 W=0 时也不保证回归基线

位置：§45、§55–57、§75。

将 W=0 代入 §56：

```text
CWND_final = max(CWND_bbr, P_bbr × MinRTT)
```

若基线因恢复或 ProbeRTT 已把 CWND 降到该乘积以下，最终窗口仍被增大。这与 §45 的精确回退冲突。开启 Assist 时直接取 max，也可能覆盖基线的损失响应和 inflight 约束。

**修订要求：**W=0 必须显式返回基线 pacing 和 CWND；辅助窗口只能经过基线的最终安全裁剪。不能从“pacing 增大”推导“允许无条件扩大窗口”。必须规定速率使用 byte/s、RTT 使用秒、CWND 使用字节；若接口使用 bit/s，则 BDP 计算需除以 8。

单纯把 CwndGain 从 2.0 调低到 1.25/1.5，不能证明更安全：在 `max(CWND_bbr, CWND_assist)` 下，调低辅助候选值可能完全不改变输出，也可能削弱应对 ACK 聚合的能力。同样，增加 `CWND_bbr + ExtraCwndBudget` 上限仍不等于遵从基线安全窗口，W=0 时也未必严格回退。首版保留基线窗口处理，只有实验确认窗口限制了有效探测，才单独设计、验证额外 inflight 预算。

### P0-4：模型持续测量，不等于模型控制行为未被破坏

位置：§8、§18、§24、§27–28、§67。

规格暂停 Startup、ProbeRTT，却没有完整约束 Drain 和 ProbeBW 的排队消退阶段。BBR 在这些阶段主动降低 pacing 是控制动作，不能一概解释为“低于目标，需要补偿”。例如 `P_bbr` 因 Drain 下降时，取 max 的辅助逻辑可能阻止队列消退。

BBRv3 草案明确包含主动加速、减速和巡航阶段，其最终控制也不只是一个带宽估计值。[BBR 草案 §5.3](https://datatracker.ietf.org/doc/html/draft-ietf-ccwg-bbr-06#section-5.3)

**修订要求：**绑定明确 BBR 版本和提交，列出允许 Assist 的阶段白名单；Drain、ProbeRTT、恢复及其他主动降速阶段由基线独占控制。先通过基线既有探测机制注入有限增益，避免把独立的 `max()` 控制器叠加在输出末端。该草案为实验性工作文档，不是已发布 RFC。

应区分“有效探测发现更高交付率”和“测量失真”。不能仅因样本来自 Assist，就禁止它提高 BtlBw；否则会丢弃真实容量增长，妨碍撤销 Assist 后由基线接管。建议记录样本所属阶段，按所选基线的采样有效性与滤波规则处理，再用撤销辅助后的持续表现验证收益。BBR 的最大带宽估计本就使用有效交付样本的窗口最大值；上述建议是据此作出的集成判断。[BBR 草案 §5.5.3–5.5.5](https://datatracker.ietf.org/doc/html/draft-ietf-ccwg-bbr-06#section-5.5.3)

### P1-1：Delivery Rate、Goodput、ACK 比例的口径混用

位置：§9–12、§58–61、§76–80。

`ACKed Delivered Bytes / Time` 需要定义哪些字节、哪个时间区间。传输层确认字节不自动等于接收应用消费的唯一有效载荷；QUIC 包确认与流字节去重尤其需要分开。按包计数的 A 与按字节计算的吞吐，在包长变化时也不一致。

简单 ACK 到达速率还可能受 ACK 压缩影响。BBR 的交付采样采用发送与确认时间区间共同约束，而非任意 ACK 窗口相除。[BBR 草案 §4.1](https://datatracker.ietf.org/doc/html/draft-ietf-ccwg-bbr-06#section-4.1)

**修订要求：**分开命名 `model_delivery_rate`、`unique_payload_goodput`、`actual_sent_rate`；Target 与唯一载荷口径一致。模型采样复用宿主采样器。A 采用同一发送批次、已判定结果的字节统计，定义迟到 ACK、误判丢失修正、重传和未知结果的处理；分母不得混入尚未确认也未判丢的包。若协议接口无法提供该统计，首版关闭损失补偿。

唯一载荷去重不等于排除重传成功的载荷。某段数据原发送丢失、经重传首次确认时，仍应计入一次有效交付；若原副本和重传副本都被确认，也只计一次。QUIC 需按流标识和字节区间去重，不能只按包是否“重传”判断。RFC 9000 规定丢失的信息可以在新的帧和包中再次发送。[RFC 9000 §13.3](https://www.rfc-editor.org/rfc/rfc9000.html#section-13.3)

`T/A` 只是损失近似独立、统计口径一致、容量有余量等假设下的尝试值，不能保证真实 goodput。A_min=0.8 与 G_max=1.25 在默认配置下提供重合的补偿上限，应说明二者用途，避免制造两个独立安全保证的印象。

### P1-2：边际效率没有因果时间对齐

位置：§36–41、§58–60。

改变 pacing 后，交付反馈需要经过传播与排队，不能拿同一时刻的速率差直接相除。CWND 或接收流控受限时，“设置的 pacing”也不是“实际多发送的速率”。在 ΔPacing≈0 时，η 会失效；交叉流量变化又可能制造虚假收益。

**修订要求：**使用可识别的探测轮次，等待对应发送批次获得反馈；记录实际发送增量，并定义最小分母、有效样本和未知状态。首版可用“有界探测之后，观察窗口内是否有足够 goodput 改善”的粗粒度规则。η 先做遥测，验证可靠性后再作为主要控制输入；在线 η 不能证明相对于没有 Assist 的反事实收益。

无有效激励、正在降速或反馈未到齐时，标记 `efficiency_valid=false`，不能用 η=1 伪造高效样本，也不直接判为失败。此时不得以效率为依据继续加码，独立时间/字节预算照常执行。保存原始有效差分和异常标记；仅做 EWMA 或把数值截断到 [0,1]，不能解决时间错位和未知状态问题。

### P1-3：控制时钟和滤波延迟未定义

位置：§25、§29–31、§35、§43、§58、§67。

如果每个 ACK 都执行 `W_new=0.25W_target+0.75W_old`，短时间内会完成多次平滑，行为不再是四个 RTT 的爬升。反之，若使用最短 500 ms 的 Fast Window，MinRTT=4 ms 时窗口覆盖 125 RTT，明显慢于 3 RTT 进入、4 RTT 爬升的描述。

**修订要求：**ACK 驱动模型更新；以 packet-timed round 或明确的时间间隔驱动辅助决策。跨时间更新使用与 Δt 有关的平滑系数，或固定每轮更新一次。紧急停用不能等待慢速窗口。分别定义测量窗口、决策周期、连续满足计数和样本过期规则。

### P1-4：规格正文、主循环和状态图不一致

位置：§24、§34–35、§43、§50–51、§67–72、§95。

正文要求可靠样本、Soft 时停止增加 W、Hard 持续 2 RTT、TargetInfeasible 抑制，但主循环没有完整体现这些条件。状态图优先判断是否接近 Target，也可能让读者误以为达到目标后不再检查拥塞。回调早退后何时发布新 pacing/CWND 没有定义。

**修订要求：**只保留一张权威状态转换表和一个权威控制循环。明确保护优先级、所有状态的进入/退出动作、截止时间、失败计数重置条件。W=0 和最终输出更新必须原子一致；AppLimited、目标变更或路径变化不能意外清除失败抑制。失败编号从 1 开始时，若首轮退避是 Base，应使用 `Base×2^(n−1)`，并在移位/乘法前做饱和处理。

### P1-5：RTT 护栏不能承诺端到端排队时延界限

位置：§32–41、§46、§63。

AND 组合的有效 Soft 阈值为 `max(0.25×MinRTT, 10 ms)`；Hard 阈值为 `max(0.5×MinRTT, 20 ms)`。MinRTT=200 ms 时，需要额外 100 ms 才触发 RTT Hard 分支；MinRTT=4 ms 时，达到 20 ms 的额外排队又已经很高。

SRTT−MinRTT 还可能包含反向路径拥塞和路径改变，不能直接视为正向瓶颈队列长度。旧 MinRTT 会让 Recovery 长期无法退出。

**修订要求：**定义可解释的额外时延预算及路径基线刷新规则，区分软趋势和紧急停止；给 Recovery 加最大停留时间，届时保持 Assist 禁用并重建基线。若宿主已提供经过验证的 ECN/恢复信号，首版必须尊重它，不能等到第四阶段才处理。不要以“支持 ECN”为由自行实现未经验证的 ECN 控制律。

把原 AND 条件改写成上述 max 形式，在数学上只是等价表达，不是新的自适应修正。若引入时延预算，还须承认它是停用阈值，并不保证经过反馈延迟后实际排队永不超限。Soft 触发后至少禁止继续增益；是否在下一决策轮衰减 W 可作为单独实验，不同时叠加多套窗口与权重调节器。

### P1-6：没有队列增长也不能证明公平

位置：§73–74、§82、§100。

竞争流主动让步时，BABR 的吞吐可能提高、η 也较高，而队列保持稳定。这表示份额转移，不一定表示发现空闲容量。只看队列和本流收益，无法落实“公平性优先于 Target”。

**修订要求：**限制辅助占空比、单次额外发送预算及多连接聚合预算；测试不同 RTT、流数量、Target 大小和先后启动。除 Jain 指数，还必须报告各流吞吐、竞争流相对双基线场景的退化和瓶颈总 goodput。Jain 指数不能单独定义异构流的公平目标。

单端通常不能可靠识别其他流的算法、数量或业务是否延迟敏感。不能把“检测到 CUBIC/实时流后退让”作为首版安全前提。测试床可由外部测量竞争流，受控部署可复用现有进程/租户聚合限制；没有相应观测或配额时，应限制试验连接数，而不是宣称具备自动竞争检测能力。

### P2：重复叙述较多，规范性定义不足

101 个章节中多处重复原则、公式和示例，但缺少参数单位、调用顺序、边界输入与完整转移表。建议重组为目标与非目标、测量、控制规则、状态机、安全不变量、宿主接口、测试与参数七部分，把说明性案例放入附录。规范公式应只有一个权威版本。

## 4. 推荐的最小实施方案

### 4.1 先锁定一个宿主与一个 BBR 基线

首选团队已有、可以修改拥塞控制接口的用户态传输栈；如果项目实际目标是 Linux TCP，则直接以该内核环境评估，不能假设用户态结果可直接迁移。当前规格没有指定协议、代码库和部署环境，因此不能给出代码接口级可行性承诺。

需要明确提供发送、ACK、丢失及恢复事件；BBR 阶段；有效交付样本；实际 bytes-in-flight；pacer；应用/接收流控受限状态；路径变化通知。BBR 必须接收实际发送行为，不应维持一套与真实发包脱节的“影子基线”。

外层封装或修改基线内部探测接口均可评估；结构选择取决于能否保证阶段约束、真实事件输入和最终安全裁剪，不能将“BBR 完全不知道 Assist”作为正确性的保证。宿主若只能覆写最终速率而无法满足这些条件，先做观测模式，不启用辅助发包。

若使用 QUIC，应保留宿主的丢包检测、PTO、持续拥塞和 pacing 处理；BABR 的 RECOVERY 建议改名 `ASSIST_BACKOFF`，避免与传输恢复混淆。RFC 9002 对持续拥塞和突发控制有明确要求，不能用 Target 覆盖。[RFC 9002 §7.6–7.7](https://www.rfc-editor.org/rfc/rfc9002.html#section-7.6)

### 4.2 先实现 Lite 探测，再决定是否加损失补偿

先增加一个默认关闭发包影响的观测模式：记录基线表现、当时的辅助触发条件和候选输出，用于检查计量、触发频率与运行开销；无需再建设一套功能重复的 dry-run 系统。观测模式不会产生真实的辅助激励，因此不能测出 Assist 的 η、AGE、真实额外交付或“启用后会达到多少吞吐”。虚拟状态只表示既有轨迹上的规则结果，不能当作闭环反事实仿真。

第一轮实验只需：成熟基线、Target 触发、有限探测预算、阶段保护、独立超时/无收益退出、失败抑制和遥测。暂不启用 `T/A`，不新增独立 CWND max 规则，也不同时支持多套 W 算法。

Lite 仍需修复 P0 问题；仅把 `P_target=T` 并不能自动获得公平性或稳定性。

建议的决策优先级：

```text
输入无效或采样过期 → 停止辅助，使用有效的基线/宿主安全路径
基线恢复、主动降速或 RTT 测量 → 精确遵从基线
应用/接收端/本机受限 → 不做网络容量辅助
明确拥塞、预算耗尽、无收益或超时 → 停止辅助并退避
仍处于抑制期 → 只运行基线
目标不足且满足样本与阶段条件 → 请求一次有界探测
```

“模型不可用时回退模型”本身不是完备的 fail-safe：仅辅助输入无效时可返回有效 BBR；若 BBR 本身不可用，必须使用宿主已有安全初始化或错误处理，不能继续使用旧目标速率。

### 4.3 修订版必须具备的不变量

1. Assist 禁用时，最终 pacing、CWND 和突发预算精确遵循基线。
2. 每次辅助有有限时间和额外注入字节上界，且不依赖 RTT 膨胀才能退出。
3. 基线主动降速与传输恢复优先于辅助；不会因辅助扩大而绕过安全裁剪。
4. 目标再大，也不能扩大已确定的单轮增长与注入预算。
5. 无样本、零分母、路径变化、应用受限均有确定行为。
6. 冷却或抑制期间不得通过状态重置立即重试；重新开放需明确事件或期限。

保留基线安全裁剪可能让某些场景没有额外收益。这应被记录为实验结果，不能通过绕过保护来证明方案有效。

### 4.4 Target 作用范围、变更与失败抑制

首版明确 Target 属于一个发送方向、一个拥塞控制实例；若宿主的多个应用流共享同一实例，不得给每个流各叠加一份 Target。多个独立连接的 Target 不自动构成主机级配额，应复用宿主既有聚合限额。首版暂不引入多路径协同和新的通用调度器。

| 事件 | 建议处理 |
| --- | --- |
| Target 缺省或为 0 | 禁用辅助；其他配置无效时拒绝配置并保持有效基线 |
| Target 增大 | 不抬升正在执行的探测预算；如取消并重启探测，须重新满足准入条件且保留原退避约束 |
| Target 降低 | 立即重算候选输出；不因旧 W 维持超出新目标需求的辅助；基线仍可超过 Target |
| 独立 MaxRate 限额变更 | 由最终发送策略统一执行，包括基线和辅助；单位与统计层次需明确，Target 不能覆盖硬限额 |
| 用户反复修改 Target | 不能清除同一路径的失败历史、预算债务或冷却期限 |
| 经确认的路径变化 | 按宿主路径规则重建模型与样本有效性；单次 RTT 波动不视为可清除限制的迁移证据 |

外部硬限速导致的低吞吐属于策略受限，不能继续触发网络容量辅助；此时记录“策略限制目标达成”，不归因于网络不可达。所谓 W=0 的基线一致性，也应在相同外部策略约束下比较。

保留用户原始 Target，不以调低 EffectiveTarget 的方式改善 TAR。首版用失败抑制解决重复追逐；如果以后加入内部可执行目标，必须同时报告原始目标达成率。冷却期间基线仍继续探测，冷却不等于停止发送；暂不因单次 delivery 翻倍提前解禁。若将来支持提前解禁，须要求连续有效样本、最低冷却时长和重试频率上限。

### 4.5 最小遥测与退出能力

在每轮决策和状态转换时记录下列字段，采用有界缓冲或采样日志，避免为每个 ACK 生成昂贵日志：

| 类别 | 最少字段 |
| --- | --- |
| 定位 | 单调时间、连接/路径标识、配置版本、BBR 阶段、BABR 状态、探测轮次 |
| 决策 | Target、W、基线/候选/最终 pacing 与 CWND、实际发送速率、inflight |
| 输入质量 | 模型交付率、唯一载荷 goodput、RTT、A、η 及各自有效性和样本年龄 |
| 预算 | 本轮参考速率、剩余时间/字节、失败计数、抑制截止时间 |
| 原因 | 目标接近、样本无效、应用/策略受限、基线保护、拥塞、无收益、预算耗尽、目标/路径变更 |

原因码只记录可观测条件，不把“已证明有容量余量”或“已发现竞争流”写成没有证据的事实。实验开关需能在下一次发送决策前关闭辅助并回到有效基线，继续维护模型；无法撤回已发送或已交给不可撤销卸载队列的数据，因此也须限制预先排程的突发量。观察模式与实际辅助模式均测 CPU、内存和日志开销。

## 5. 验证计划与验收门槛

### 5.1 对照组

在相同宿主、版本、机器和路径上至少比较：原版 BBR、BABR-Lite、完整 BABR。Brutal 可作为已知容量场景中的参考组；有条件时增加更成熟 BBR 版本作为对照，但跨实现结果不得归因于单一算法变化。

必须报告相同总发送预算下的收益，并区分瞬态恢复与稳态吞吐。不能把更高的总发送量或其他流的退让直接解释为网络效率提升。

### 5.2 最小测试矩阵

| 类别 | 关键用例 | 主要目的 |
| --- | --- | --- |
| 规格反例 | 无 RTT 增长的 policer；超大 T；W=0 且基线窗口收缩 | 验证 P0 修订 |
| 静态链路 | 多组 RTT、容量和 0/1/5% 随机丢包 | 区分正常探测与损失补偿收益 |
| 容量变化 | 500→100→500 Mbps；阶跃与逐步变化 | 测恢复时间、过冲与重复失败 |
| 队列机制 | 浅/深 FIFO、AQM/ECN、按流调度；明确区分 policer 与 shaper | 避免只在单一队列模型下有效 |
| 反馈异常 | ACK 压缩、延迟 ACK、重排、突发丢包、反向拥塞 | 验证采样与误判 |
| 发送瓶颈 | AppLimited、接收流控、CPU/pacer 受限、空闲重启 | 避免错误辅助 |
| 多流 | BABR/BBR、BABR/CUBIC、BABR/BABR；不同 RTT、Target 和流数 | 测公平性和聚合攻击性 |
| 路径变化 | RTT 基线升降、迁移、短时中断 | 验证抑制与基线重建 |
| 动态配置 | Target 上调/下调/关闭、反复变更、硬限额低于 Target | 验证预算和退避不被配置绕过 |
| 无反馈 | 停止 ACK、PTO、持续拥塞 | 验证无需新 ACK 也能停用辅助，且不破坏宿主恢复 |
| 学习与交接 | 确有容量增长、Assist 撤销、之后继续超过 Target | 验证真实样本可学习，且交接后没有虚假高估造成持续过冲 |
| 软件开销 | 短流、观测模式、日志开关、高 ACK 频率、在线关闭辅助 | 验证收益不被计算/日志成本抵消与退出能力 |

仿真/受控链路需公开拓扑、队列大小、损失位置、MTU、卸载配置、随机种子和依赖提交。首轮可对关键场景做至少 10 次独立运行，报告分布和置信区间；不足以区分收益时增加样本，而不是只挑最佳结果。

专项回归覆盖有效载荷经重传才首次交付、重复确认不重复计数、无效 η 不参与增益判断、Drain/ProbeRTT 期间实际 inflight 的下降。阈值敏感性至少包含毫秒级和数百毫秒级 RTT。可以使用仿真或最小受控链路先跑反例，工具不限定为某个框架；观测模式、闭环仿真和真实发包测试的证据不能互相替代。

### 5.3 建议的立项门槛

以下是便于作出 Go/No-Go 决策的初始工程门槛，需结合业务确认，不是业界标准或已验证参数。

| 维度 | 建议门槛 |
| --- | --- |
| 主要收益 | 预先指定场景中，稳态唯一载荷 goodput 提高 ≥10%，或容量恢复后达到 0.9T 的时间降低 ≥20%；差异需超出运行噪声 |
| 正常场景 | 无丢包且目标可达时，goodput 退化不超过 3% |
| 延迟成本 | 同一路径相对基线的 p95 RTT 增量不超过预先约定预算；初始可用 max(5 ms, 10%×基线 p95 RTT) 筛选 |
| 发送成本 | 每交付一个唯一载荷字节的发送字节成本增幅不超过 10%，除非业务明确接受更高成本 |
| 公平性 | 在总 goodput 未提高时，竞争流相对双基线对照的吞吐损失不超过 10%；逐流报告，不用平均值掩盖饥饿 |
| 失败处理 | 全部不可达目标场景在规定辅助预算内退出，包括没有 RTT 增长的场景 |
| 安全一致性 | 所有禁止辅助的阶段，输出与基线一致；无预算绕过、NaN、溢出或持续重试 |

TAR 的分母需定义为具有足够应用发送需求的有效时间；另报整体业务时间，避免删去不利样本。不可达目标的 TAR 本来就可能为零，应主要评价稳定性与退出成本。Time To Target 对未达标样本报告“未达到/超时”，不能剔除后再平均。AGE 的反事实基线来自配对实验，不能直接用在线前后差值替代。

## 6. 实施成本与推进顺序

以下估计假设由一名熟悉拥塞控制的工程师执行，已有可修改传输栈和 Linux 实验环境；是规划量级，不是交付承诺。

| 阶段 | 交付物 | 估计投入 | 继续条件 |
| --- | --- | --- | --- |
| 规格收敛 | 基线版本、计量口径、状态表、预算与 P0 反例 | 3–5 人日 | 所有 P0 有明确规则 |
| Lite 原型 | 基线集成、观测模式、原因码、预算执行与可重复反例测试 | 1–2 人周 | 安全路径一致，无无界辅助 |
| 对照实验 | 数据、重复运行、统计与失败分析 | 2–4 人周 | Lite 已有可复现增量收益 |
| 损失补偿 | 字节统计、独立消融、扩大测试 | 1–2 人周 | 完整版显著优于 Lite |

首轮研究原型与评估约 4–8 人周。若需从零移植 BBR、开发内核模块或搭建高精度发包环境，应另行估算；生产化、长期维护与跨平台验证不包含在内。

**立项建议：批准规格收敛和 Lite 原型阶段；用实验结果决定后续投入。** 最先验证的不是“能否达到 200 Mbps”，而是“是否能在有限成本内比选定基线更快发现可用容量，并在没有收益时可靠停止”。若不能同时做到这两点，继续增加复杂度没有实施价值。
